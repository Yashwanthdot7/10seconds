# Two Pointers

Two Pointers is a pattern where you maintain two index variables (left and right) that moves through an array.
One pointer starts from left, and another starts from the right.

- Used to reduce the time complexity for processing strings and arrays.
  
- **When to use:**
  - Sorted arrays
  - Palindromes
  - Finding pairs with target sum or product
  - Remove duplicates