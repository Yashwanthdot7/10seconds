# Linked Lists

- Linked lists are stored in non-continuous memory locations.
- Linked lists are dynamic.
- There is no indexing in linked lists.

*Example Representation:*

```Java
    public class ListNode {
        int val;
        ListNode next;
    }
```